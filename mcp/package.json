{
  "name": "strtt-mcp",
  "version": "0.1.0",
  "description": "MCP server that lets an assistant drive strtt (Segger RTT over ST-LINK) to capture and interact with device RTT logs",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "strtt-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
